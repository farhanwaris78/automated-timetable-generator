# Runtime dependencies - UTF-8 encoded (the original file was UTF-16 and
# could not be read by `pip install -r`).
Flask==3.1.0
Werkzeug==3.1.3
Jinja2==3.1.6
MarkupSafe==3.0.2
itsdangerous==2.2.0
click==8.1.8
blinker==1.9.0
SQLAlchemy==2.0.39
waitress==3.0.2
openpyxl==3.1.5
python-dotenv==1.0.1

# Optional: only needed when you point the app at Microsoft SQL Server.
# Install it manually with `pip install pyodbc==5.2.0` if you use MSSQL.
