{
  "name": "timetable-frontend-tests",
  "private": true,
  "version": "2.0.2",
  "description": "Headless DOM tests for the timetable drag & drop engine.",
  "scripts": {
    "test": "node drag-and-drop.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
